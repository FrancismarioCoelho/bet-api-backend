image: maven:3.8-openjdk-22

stages:
  - build
  - test
  - deploy

cache:
  paths:
    - .m2/repository/

build:
  stage: build
  script:
    - echo "Compilando o projeto..."
    - mvn clean package
  artifacts:
    paths:
      - target/

test:
  stage: test
  script:
    - echo "Executando testes..."
    - mvn test
  artifacts:
    paths:
      - target/surefire-reports/

deploy:
  stage: deploy
  script:
    - echo "Realizando o deployment da aplicação..."
    - mvn deploy -DskipTests
  environment:
    name: production
    url: http://your-production-url.com
  only:
    - main
